using Gtk 4.0;

template RestartPage : Box {
  name: _("Restarting");
  orientation: vertical;
  spacing: 18;

  Spinner spinner {
    valign: center;
    margin-top: 18;
    width-request: 64;
    height-request: 64;
    spinning: false;
  }
}
