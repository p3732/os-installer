using Gtk 4.0;
using Adw 1;

template PageWrapper : Box {
  Adw.Clamp {
    maximum-size: 420;
    tightening-threshold: 420;
    child:
    Box {
      orientation: vertical;
      spacing: 12;
      styles ["clamped"]

      Adw.Bin content {
        hexpand: true;
      }
    };
  }
}
