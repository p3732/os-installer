using Gtk 4.0;
using Adw 1;

template SoftwareRow : Adw.ActionRow {
  activatable: true;
  focusable: true;

  [prefix]
  Image icon {
    width-request: 32;
    height-request: 32;
    valign: center;
  }

  [suffix]
  Switch switch {
    focusable: false;
    halign: end;
    valign: center;
  }
}
